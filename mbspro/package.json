{
  "name": "mbspro",
  "version": "1.0.0",
  "description": "MBSPro - Monorepo with Next.js frontend and NestJS backend",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --recursive run dev",
    "build": "pnpm --recursive run build",
    "lint": "pnpm --recursive run lint",
    "test": "pnpm --recursive run test",
    "seed": "pnpm --filter=api run seed",
    "clean": "node scripts/clean.js",
    "fresh": "pnpm clean && pnpm install",
    "reset": "pnpm clean && pnpm install && pnpm seed",
    "eval": "node scripts/eval.js",
    "check:day2": "pnpm --filter @mbspro/api test && pnpm eval"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}
